import { View, Text } from 'react-native'
import React from 'react'
import { ScrollView } from 'react-native-gesture-handler'

import Media from '../../components/ContentComponent/Media'

const MediaScreen = () => {
  return (
    <ScrollView>
     
      <Media/>
    </ScrollView>
  )
}

export default MediaScreen